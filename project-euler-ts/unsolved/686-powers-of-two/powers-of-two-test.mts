import { describe } from "mocha";
// import { Problem686 } from "./powers-of-two.mjs";
// import { assert } from "chai";

describe("Problem686.solve", () => {
	// tests commented out due to long runtime

	it("can return the first power of two that starts with the digits '12'", () => {
		// const result = Problem686.solve(12n, 1n);
		// assert.equal(result, 7n);
	});
	it("can return the second power of two that starts with the digits '12'", () => {
		// const result = Problem686.solve(12n, 2n);
		// assert.equal(result, 80n);
	});
});
